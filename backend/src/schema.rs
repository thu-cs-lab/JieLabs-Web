table! {
    users (id) {
        id -> Integer,
        user_name -> Text,
        password -> Text,
        real_name -> Nullable<Text>,
        class -> Nullable<Text>,
        student_id -> Nullable<Text>,
    }
}
